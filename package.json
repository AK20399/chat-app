{
    "name": "root",
    "private": true,
    "version": "1.0.0",
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "start": "lerna run start",
        "test": "lerna run test",
        "dev": "lerna run dev",
        "prepare": "husky install",
        "clean": "lerna clean",
        "bootstrap": "lerna bootstrap"
    },
    "lint-staged": {
        "src/**/*.{js,jsx,ts,tsx,json,css}": [
            "prettier --write",
            "eslint --fix src/ && eslint --fix public/",
            "git add"
        ]
    }
}
